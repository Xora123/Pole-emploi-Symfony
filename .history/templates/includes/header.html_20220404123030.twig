<body>
	{% for post in posts %}

		<div class="col-md-12">
			<div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
				<div class="col p-4 d-flex flex-column">
					<div class="d-flex justify-content-between">
						<strong class="p-2">Type de l'offre : {{ post.type }}</strong>
						<p class="p-2">{{ post.duree }}
							durée
						</p>
					</div>
					<h3 class="mb-0">{{ post.title }}</h3>

					<p class="mb-auto">{{ post.content }}</p>
					<div class="mb-1 text-muted">{{ post.departement }}</div>
					<div class="mb-1 text-muted">{{ post.createdAt|date("m/d/Y") }}</div>

					<a href="{{ path('app_post', {'id':post.id} )}}" class="">Continue reading</a>
				</div>
				<div class="col-auto d-none d-lg-block"></div>
				
			</div>
		</div>
	</div>
</body>
</div>{% endfor %}
	{{ knp_pagination_render(posts) }}</body></div></div><footer class="blog-footer"><p><a href="#">Back to top</a></p></footer></body>
